Every real buttons (Game mode button, Difficulty Selection button and the Play button) has the same style, but it is declared every time in there own script. This could probably be refactored in the same class. 

Move the parameter recuperation samewhere else, possibly accessible from a static class (as Style), to remove the call to FindWithTag in too many classes.